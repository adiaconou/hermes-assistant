# Test environment configuration
# These are fake values for testing - no real API calls are made

NODE_ENV=test

# Fake Anthropic API key (mocked in tests)
ANTHROPIC_API_KEY=test-api-key

# Fake Twilio credentials (mocked in tests)
TWILIO_ACCOUNT_SID=test-account-sid
TWILIO_AUTH_TOKEN=test-auth-token
TWILIO_PHONE_NUMBER=+15555550000

# Local base URL
BASE_URL=http://localhost:3000

# Use local/memory providers for tests
UI_STORAGE_PROVIDER=local
UI_SHORTENER_PROVIDER=memory
UI_LOCAL_STORAGE_PATH=./data/test-pages
