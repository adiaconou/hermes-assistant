/**
 * Memory domain bridge — re-exports memory store for scheduler use.
 */
export { getMemoryStore } from '../../memory/runtime/index.js';
export type { MemoryStore, UserFact } from '../../memory/types.js';
