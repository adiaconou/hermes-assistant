# Domain & Agent Catalog

> Auto-generated by `npm run docs:agents`. Do not edit manually.

Generated: 2026-02-22

## Summary

| Domain | Exposure | Agent | Tools | Layers |
|--------|----------|-------|-------|--------|
| calendar | agent | calendar-agent | 5 | types, providers, runtime |
| drive | agent | drive-agent | 16 | types, providers, runtime |
| email | agent | email-agent | 3 | types, providers, runtime |
| email-watcher | tool-only | — | 6 | types, repo, providers, service, runtime |
| google-core | internal | — | — | types, providers, service |
| memory | agent | memory-agent | 4 | types, repo, providers, service, runtime |
| scheduler | agent | scheduler-agent | 4 | types, repo, providers, service, runtime |
| ui | agent | ui-agent | 1 | types, providers, runtime |
| general *(top-level)* | agent | general-agent | all | — |

---

## calendar (`agent`)

**Agent:** `calendar-agent`
**Description:** Manages Google Calendar events. Use for viewing, creating, updating, or deleting calendar events.
**Source:** `src/domains/calendar/runtime/agent.ts`

**Example prompts:**
- "What's on my calendar today?"
- "Schedule a meeting tomorrow at 2pm"
- "Cancel my 3pm appointment"
- "Move my dentist appointment to next week"
- "What am I doing this week?"

**Layers:** types → providers → runtime

**Cross-domain dependencies:**
- → **google-core** via `providers/google-core.ts` — Shared Google OAuth2 and retry infrastructure

**Tools:**

| Tool | Description |
|------|-------------|
| `get_calendar_events` | Get events from the user's Google Calendar. |
| `create_calendar_event` | Create a new event on the user's Google Calendar. |
| `update_calendar_event` | Update an existing event on the user's Google Calendar. |
| `delete_calendar_event` | Delete an event from the user's Google Calendar. |
| `resolve_date` | Resolve natural language dates to ISO 8601 strings (with timezone offset). |

---

## drive (`agent`)

**Agent:** `drive-agent`
**Description:** Manages Google Drive files, Sheets, and Docs. Analyzes and processes images and documents. Use for file storage, spreadsheet tracking, document creation, and image analysis.
**Source:** `src/domains/drive/runtime/agent.ts`

**Example prompts:**
- "Save this image to my Drive"
- "Create a spreadsheet to track expenses"
- "What files are in my Hermes folder?"
- "Create a document for meeting notes"
- "What is this document?"

**Layers:** types → providers → runtime

**Cross-domain dependencies:**
- → **google-core** via `providers/google-core.ts` — Shared Google OAuth2 and folder hierarchy

**Tools:**

| Tool | Description |
|------|-------------|
| `upload_to_drive` | Upload a file or image to the user's Google Drive (in the Hermes folder). |
| `list_drive_files` | List files in the user's Hermes folder or a subfolder. |
| `create_drive_folder` | Create a folder in the Hermes hierarchy. |
| `read_drive_file` | Read text content from a file in Drive. |
| `search_drive` | Search for files by name or type in the Hermes folder. |
| `get_hermes_folder` | Get the ID and link to the user's Hermes folder (creates it if needed). |
| `create_spreadsheet` | Create a new Google Spreadsheet in the Hermes folder. |
| `read_spreadsheet` | Read a range of cells from a Google Spreadsheet. |
| `write_spreadsheet` | Write data to a specific range of cells in a Google Spreadsheet. |
| `append_to_spreadsheet` | Append rows to a Google Spreadsheet. |
| `find_spreadsheet` | Find a Google Spreadsheet by name in the Hermes folder. |
| `create_document` | Create a new Google Document in the Hermes folder. |
| `read_document` | Read the content of a Google Document. |
| `append_to_document` | Append text to the end of a Google Document. |
| `find_document` | Find a Google Document by name in the Hermes folder. |
| `analyze_image` | Analyze an image using AI vision. |

---

## email (`agent`)

**Agent:** `email-agent`
**Description:** Searches and reads Gmail. Manages email watching skills that automatically process incoming emails (log to spreadsheets, send notifications). Can search by sender, subject, date, and keywords.
**Source:** `src/domains/email/runtime/agent.ts`

**Example prompts:**
- "Do I have any unread emails?"
- "Check my email from John"
- "Find emails about the project"
- "What emails did I get today?"
- "Read the email from my boss"
- "Find my hotel confirmation for my trip to Arizona"
- "Search for flight bookings from last year"
- "Show my email skills"
- "Start tracking job application emails in a spreadsheet"
- "Disable the expense tracker"
- "Pause email watching"
- "Test the tax tracker on my recent emails"

**Layers:** types → providers → runtime

**Cross-domain dependencies:**
- → **google-core** via `providers/google-core.ts` — Shared Google OAuth2

**Tools:**

| Tool | Description |
|------|-------------|
| `get_emails` | Search and list emails from Gmail. |
| `read_email` | Read a specific email by ID. |
| `get_email_thread` | Get all emails in a conversation thread. |

---

## email-watcher (`tool-only`)

*No agent — tools are attached to other agents.*

**Layers:** types → repo → providers → service → runtime

**Cross-domain dependencies:**
- → **google-core** via `providers/google-core.ts` — Shared Google OAuth2 for Gmail history sync
- → **memory** via `providers/memory.ts` — Email-watcher classifier and actions need memory store for user facts
- → **email** via `providers/email.ts` — Email-watcher test tool uses listEmails from email domain

**Tools:**

| Tool | Description |
|------|-------------|
| `create_email_skill` | Create a new email watching skill. |
| `list_email_skills` | List all email watching skills for the current user. |
| `update_email_skill` | Update an existing email watching skill by name. |
| `delete_email_skill` | Delete an email watching skill by name. |
| `toggle_email_watcher` | Enable or disable the email watcher for the current user. |
| `test_email_skill` | Dry-run a skill against recent emails. |

---

## google-core (`internal`)

*Internal infrastructure — no agent or tools exposed.*

**Layers:** types → providers → service

---

## memory (`agent`)

**Agent:** `memory-agent`
**Description:** Manages user memory and facts. Use for storing, viewing, or deleting personal facts.
**Source:** `src/domains/memory/runtime/agent.ts`

**Example prompts:**
- "Remember that I like black coffee"
- "What do you know about me?"
- "Forget that I have a cat"
- "Update my preference to decaf"

**Layers:** types → repo → providers → service → runtime

**Tools:**

| Tool | Description |
|------|-------------|
| `extract_memory` | Store a new fact about the user. |
| `list_memories` | List stored facts about the user. |
| `update_memory` | Update an existing fact by ID. |
| `remove_memory` | Delete a stored fact by ID. |

---

## scheduler (`agent`)

**Agent:** `scheduler-agent`
**Description:** Manages reminders and scheduled tasks. Use for creating, viewing, updating, or deleting reminders and recurring messages.
**Source:** `src/domains/scheduler/runtime/agent.ts`

**Example prompts:**
- "Remind me to call mom tomorrow at 5pm"
- "Set a daily reminder at 8am to take vitamins"
- "What reminders do I have?"
- "Cancel my morning reminder"
- "Change my gym reminder to 7am"

**Layers:** types → repo → providers → service → runtime

**Cross-domain dependencies:**
- → **memory** via `providers/memory.ts` — Scheduler executor needs memory store for user fact retrieval

**Tools:**

| Tool | Description |
|------|-------------|
| `create_scheduled_job` | Create a scheduled message that will be generated and sent to the user. |
| `list_scheduled_jobs` | List all scheduled jobs for the current user. |
| `update_scheduled_job` | Update an existing scheduled job. |
| `delete_scheduled_job` | Delete a scheduled job permanently. |

---

## ui (`agent`)

**Agent:** `ui-agent`
**Description:** Generates interactive web pages. Use for lists, forms, calculators, or any content that benefits from visual/interactive presentation. IMPORTANT: This agent CANNOT fetch external data (no network access). To display live data (weather, calendar events, etc.), first fetch the data with another agent, then pass it to ui-agent to render.
**Source:** `src/domains/ui/runtime/agent.ts`

**Example prompts:**
- "Create a shopping list I can check off"
- "Make a calculator for tip splitting"
- "Build a form for my event RSVP"
- "Generate a weekly planner"
- "Create a timer for my workout"
- "Display calendar events in an interactive view (requires calendar data from previous step)"

**Layers:** types → providers → runtime

**Tools:**

| Tool | Description |
|------|-------------|
| `generate_ui` | Host an HTML page and return its URL. |

---

## general *(top-level fallback)*

**Agent:** `general-agent`
**Description:** Handles all tasks using the full tool suite. Use when no specialized agent fits or for multi-domain requests.
**Source:** `src/agents/general/index.ts`
**Tools:** all (`*`)

**Example prompts:**
- "General questions and conversations"
- "Tasks spanning multiple domains"
- "Fallback for unclassified requests"

---

## Shared Tools *(top-level, no domain)*

These tools live in `src/tools/` and are not owned by any domain.

| Tool | Description |
|------|-------------|
| `format_maps_link` | Convert an address or location to a Google Maps link, returning plain text  |
| `set_user_config` | Update user profile (name, timezone). |
| `delete_user_data` | Delete all stored data for the user (config, memory, conversation, scheduled jobs). |

---

## Agent Registry Order

Order in `src/registry/agents.ts` (planner sees agents in this order):

1. calendar
2. scheduler
3. email
4. memory
5. ui
6. drive
7. general

