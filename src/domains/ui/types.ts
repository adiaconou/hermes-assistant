/**
 * @fileoverview UI domain type definitions.
 */

// Re-export types from existing UI service for convenience
export type { GeneratePageOptions, GeneratePageResult, GeneratePageSuccess, GeneratePageError } from '../../services/ui/index.js';
